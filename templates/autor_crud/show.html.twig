{% extends 'base.html.twig' %}

{% block title %}Autor{% endblock %}

{% block body %}
    <h1>Autor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ autor.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ autor.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_autor_crud_index') }}">back to list</a>

    <a href="{{ path('app_autor_crud_edit', {'id': autor.id}) }}">edit</a>

    {{ include('autor_crud/_delete_form.html.twig') }}
{% endblock %}
