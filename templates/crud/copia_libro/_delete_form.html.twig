<form method="post" action="{{ path('app_copia_libro_crud_delete', {'id': libro.id, 'copy_id': copia_libro.id}) }}" onsubmit="return confirm('Estás seguro de eliminar esta copia?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ copia_libro.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
