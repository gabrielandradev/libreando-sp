{% extends 'base.html.twig' %}

{% block title %}Descriptor index{% endblock %}

{% block body %}
    <h1>Descriptor index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for descriptor in descriptors %}
            <tr>
                <td>{{ descriptor.id }}</td>
                <td>{{ descriptor.nombre }}</td>
                <td>
                    <a href="{{ path('app_descriptor_crud_show', {'id': descriptor.id}) }}">show</a>
                    <a href="{{ path('app_descriptor_crud_edit', {'id': descriptor.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_descriptor_crud_new') }}">Create new</a>
{% endblock %}
